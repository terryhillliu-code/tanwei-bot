version: "3.8"

services:
  tanwei-bot:
    build: .
    container_name: tanwei-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./config:/app/config
      - ./templates:/app/templates
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Shanghai
